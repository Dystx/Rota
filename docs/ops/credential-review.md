# Credential Review

Before a production deployment, the release owner records whether a private
history review found any exposed credentials and whether the affected values
were rotated. This file must never contain a credential, project reference,
connection URL, token, or other secret-bearing value.
