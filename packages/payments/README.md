# packages/payments

This package now exposes deterministic checkout-plan data for:

- free preview
- paid trip unlock
- human polish review add-on

It is still provider-free. Stripe session creation and webhook handling remain future work.
