# Workers

This app now includes the first worker-plan contract in `src/index.ts`.

Current scope:

- markdown export job planning
- reviewer assignment / completion job planning
- route validation refresh planning

This is still deterministic planning only. No queue runner or external worker runtime is attached yet.
