import { OperatorLoading } from "../_components/operator-loading";

/** Stable operator shell while an admin route streams. */
export default function AdminLoading() {
  return <OperatorLoading section="admin" />;
}
