import * as React from "react";
import Link from "next/link";
import { PageShell, SectionHeading } from "@repo/ui";

const topics: Array<[string, string, string]> = [["Planning a route", "Start or refine a Portugal brief in the planner.", "/planner"], ["Payment and unlock", "Check a recent itinerary’s unlock state after payment.", "/itineraries"], ["Exports", "Open an unlocked trip to see available export formats and retry guidance.", "/itineraries"], ["Local review", "Review is optional and available after itinerary unlock.", "/local-expertise"], ["Account access", "Use a private email link to return to your saved work.", "/sign-in"]];
export default function SupportPage() { return <PageShell bare><SectionHeading eyebrow="Support" title="Find the next useful step." description="Choose the part of your planning journey you need help with." h1 /><div className="grid gap-5 md:grid-cols-2">{topics.map(([title, text, href]) => <Link key={title} href={href} className="border-t border-[var(--color-border)] py-5 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ochre-light"><h2 className="font-display text-2xl text-primary">{title}</h2><p className="mt-2 text-sm text-on-surface-variant">{text}</p></Link>)}</div></PageShell>; }
