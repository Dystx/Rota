import { OperatorLoading } from "../_components/operator-loading";

/** Stable operator shell while a reviewer route streams. */
export default function ReviewerLoading() {
  return <OperatorLoading section="reviewer" />;
}
