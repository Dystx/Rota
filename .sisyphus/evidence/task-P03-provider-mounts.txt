=== task-P03 MotionProvider evidence ===
File: packages/ui/src/components/motion-provider.tsx

Contents:
---
'use client';

import type { ReactNode } from 'react';
import { LazyMotion, domAnimation } from 'motion/react';

type MotionProviderProps = {
  children: ReactNode;
};

export function MotionProvider({ children }: MotionProviderProps) {
  return (
    <LazyMotion features={domAnimation} strict>
      {children}
    </LazyMotion>
  );
}
---

Verification:
- 'use client' directive: present (line 1)
- Imports from 'motion/react': LazyMotion, domAnimation (both in allowlist)
- Wraps children in <LazyMotion features={domAnimation} strict>
- Strict mode enabled — forces use of `m` components from 'motion/react-m'
- Default export of named function MotionProvider with typed props
- Passes motion-gate (verified by clean run, scanned 107 files, 0 violations)
